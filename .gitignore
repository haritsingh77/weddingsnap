# Python
backend/venv/
backend/__pycache__/
backend/app/__pycache__/
**/__pycache__/
*.pyc
*.pyo

# Environment
.env
*.env

# Face encodings cache (large file, don't commit)
*.pkl
*.npy
encodings/
.venv/
# Node
frontend/node_modules/
frontend/dist/

# OS
.DS_Store
Thumbs.db

# Google credentials
credentials.json
token.json
backend/service-account.json
backend/.env

# Local Cache & Temp Directories
cache/
cache_ssd/
temp_preprocess/
backend/cache/
backend/encodings/
backend/temp_preprocess/
cluster_review/
scratch/

# Log files
*.log
backend/*.log


# Claude Code: keep launch.json, exclude local agent worktrees & state
.claude/worktrees/
.claude/settings.local.json

# Env backups (contain secrets - must never be committed)
backend/.env.*
*.env.bak*

# Eval fixtures: real guests' selfies + derived encodings, not source
eval/

# Face crops + name proposals: real guests' faces and names, not source
cluster_faces*/
cluster_name_proposals.json
cluster_names_by_photos.json

# DB/state backups: guest names, phones, selfie images
backups/
guest_links.csv
